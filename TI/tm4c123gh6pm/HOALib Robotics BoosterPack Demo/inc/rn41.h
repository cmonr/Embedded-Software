#include "uart.h"
/*
class RN41
{
    public:
        RN41( tUART* );

        void write( unsigned char );

        void enable( void );
        void disable( void );

    private:
        tUART* _uart;
};*/
